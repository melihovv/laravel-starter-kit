{
  "private": true,
  "scripts": {
    "test": "npm run php-test",
    "php-test": "phpunit"
  },
  "devDependencies": {
    "ghooks": "^1.3.2"
  },
  "config": {
    "ghooks": {
      "pre-commit": "npm run test"
    }
  }
}
